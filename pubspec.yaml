name: app3
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.0
  path_provider: ^2.1.6
  path: ^1.9.0
  cupertino_icons: ^1.0.8
  file_picker: ^13.1.0
  table_calendar: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true